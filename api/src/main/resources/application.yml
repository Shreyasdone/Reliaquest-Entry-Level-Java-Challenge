spring:
  application:
    name: entry-level-java-challenge
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: none
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  security:
    user:
      name: user
      password: password
server:
  port: 8080
